# Allow Electron postinstall script to download its binary even in CI / Docker.
allowed-scripts=electron
# Allow every package’s *preinstall/postinstall* hooks (Electron depends on
# them).  pnpm ≥10 disables these by default unless explicitly re-enabled.
enable-pre-post-scripts=true

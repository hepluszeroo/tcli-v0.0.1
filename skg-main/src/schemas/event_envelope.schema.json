{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "EventEnvelope",
  "description": "Common envelope structure for all events",
  "type": "object",
  "required": ["event_id", "event_type", "timestamp", "source_service"],
  "properties": {
    "event_id": {
      "type": "string",
      "description": "Unique identifier for this event",
      "format": "uuid"
    },
    "event_type": {
      "type": "string",
      "description": "Type of the event"
    },
    "timestamp": {
      "type": "string",
      "description": "ISO 8601 timestamp of when the event was generated",
      "format": "date-time"
    },
    "source_service": {
      "type": "string",
      "description": "Name of the service that generated this event"
    },
    "correlation_id": {
      "type": "string",
      "description": "Optional ID to correlate events in a sequence",
      "format": "uuid"
    }
  }
}